Maya basic example

Run locally:

1) pnpm install
2) pnpm build
3) pnpm run maya -- dev --config examples/basic/maya.config.ts

Fallback:

- node dist/cli.mjs dev --config examples/basic/maya.config.ts

Then open http://localhost:8080
